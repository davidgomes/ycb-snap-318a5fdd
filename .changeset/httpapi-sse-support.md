---
"@effect/platform": patch
---

Add Server-Sent Events (SSE) support to the HttpApi framework, including `HttpApiEndpoint.sse`, `HttpApiSchema.withSSE`, the `HttpApiSSE` module, streaming handlers, typed client streams, and OpenAPI `text/event-stream` documentation.
